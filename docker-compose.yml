services:
  orchestrator:
    container_name: orchestrator
    build: 
      context: src/PC4
      dockerfile: Dockerfile
    environment:
      WORKER_TYPE: "orchestrator"
      WORKER_PORT: "8040"
      API_PORT: "8080"
      MONGODB_URI: "mongodb://hello:world@localhost:27017"
      REDIS_URL: "redis:6379"
      MOVIE_CSV_PATH:  "/data/csv/movies.csv"
      RATING_CSV_PATH: "/data/csv/ratings.csv"
      TAGS_CSV_PATH:   "/data/csv/tags.csv"
      LINKS_CSV_PATH:   "/data/csv/links.csv"
    ports:
      - 8080:8080
      - 8040:8040
    volumes: 
      - ./data/ml-25m:/data/csv/
  worker:
    build: 
      context: src/PC4
      dockerfile: Dockerfile
    environment:
      WORKER_TYPE: "worker"
      MONGODB_URI: "mongodb://hello:world@localhost:27017"
      ORCHESTRATOR_IP: "orchestrator:8040"
  mongo:
    container_name: mongo
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: pass
      MONGO_INITDB_DATABASE: default
    ports:
      - 27017:27017
    volumes: 
      - ./data/mongo:/data/db/

  redis:
    container_name: redis
    image: redis:latest
    restart: always
    ports:
      - "6379:6379"

volumes:
    cache:
      driver: local